{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Event and native menu capability for the read-only overlay window",
  "windows": ["overlay"],
  "permissions": ["core:event:default", "core:menu:default"]
}
